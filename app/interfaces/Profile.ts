export interface Profile {
  id: string;
  firstName: string;
  lastName: string;
  email: string;
  mobileNumber: string;
  profilePicture: string;
  permanentAddress: string;
  currentAddress: string;
};